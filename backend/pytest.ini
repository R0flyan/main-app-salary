[pytest]
pythonpath = .
testpaths = tests
addopts = -ra --strict-markers --cov=app --cov-report=term-missing --cov-fail-under=75
markers =
    unit: fast isolated unit tests
    integration: integration tests with app and database
    e2e: end-to-end browser tests
